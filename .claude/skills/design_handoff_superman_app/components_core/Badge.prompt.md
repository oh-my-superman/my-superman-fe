Small status pill.

```jsx
<Badge variant="success" dot>동행 중</Badge>
<Badge variant="accent">예약 22:00</Badge>
<Badge variant="danger" dot>위급</Badge>
```

Variants: `default` (coral) · `secondary` · `outline` · `success` · `danger` · `warning` · `accent`. Add `dot` for a leading status dot.
